VITE_API_URL=http://localhost:8000
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# DO NOT PIN a specific model here. A pinned free-tier model was
# silently removed from OpenRouter's free tier in production, causing
# a live 404 outage (see git history: config was pinned then reverted
# twice). Only "openrouter/free" is supported going forward.
OPENROUTER_MODEL=openrouter/free
