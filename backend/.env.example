# API
PATHFINDER_ENV=development
PATHFINDER_CORS_ORIGINS=http://localhost:5173

# Supabase (configured before protected API endpoints are enabled)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_JWT_AUDIENCE=authenticated

# LLM (used only for constrained explanation and pacing in a later milestone)
OPENAI_API_KEY=
OPENAI_MODEL=
